rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /giveaway_participants/{participantId} {
      allow read: if true;
      allow write: if false; // Only allow writes from the backend bot
    }
  }
}
