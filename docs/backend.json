{
  "entities": {
    "giveaway": {
      "title": "Giveaway",
      "description": "Represents a single giveaway event.",
      "type": "object",
      "properties": {
        "title": {
          "type": "object",
          "properties": {
            "en": { "type": "string" },
            "es": { "type": "string" }
          }
        },
        "prizeImage": {
          "type": "string",
          "format": "uri"
        },
        "active": {
          "type": "boolean",
          "description": "Indicates if the giveaway is currently active."
        },
        "participants": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "id": { "type": "string", "description": "Discord User ID" },
              "name": { "type": "string", "description": "Discord Username" }
            },
            "required": ["id", "name"]
          }
        },
        "winner": {
          "type": "object",
          "properties": {
            "id": { "type": "string" },
            "name": { "type": "string" }
          }
        }
      },
      "required": ["title", "active", "participants"]
    }
  },
  "auth": {
    "providers": []
  },
  "firestore": {
    "/giveaways/{giveawayId}": {
      "schema": {
        "$ref": "#/entities/giveaway"
      },
      "description": "Stores active and past giveaway events."
    }
  }
}
